<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tk.tliaswebmanagment.Mapper.EmpExprMapper">
    <insert id="insertBatch">
        insert into emp_expr (emp_id , company, job, begin, end)
        values
        <foreach collection="exprList" item="empExpr" separator=",">
            (#{empExpr.empId},#{empExpr.company}, #{empExpr.job}, #{empExpr.begin}, #{empExpr.end})
        </foreach>
    </insert>
    <delete id="deleteExprByEmpIds">
        delete from emp_expr where emp_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
</mapper>