<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tk.tliaswebmanagment.Mapper.StudentMapper">
    <update id="updateById">
        update student
        <set>
            <if test="name != null and name != ''">
                name=#{name},
            </if>
            <if test="no != null and no != ''">
                no=#{no},
            </if>
            <if test="gender != null">
                gender=#{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone=#{phone},
            </if>
            <if test="degree != null">
                degree=#{degree},
            </if>
            <if test="clazzId != null">
                clazz_id=#{clazzId},
            </if>
            <if test="idCard != null and idCard != ''">
                id_card=#{idCard},
            </if>
            <if test="isCollege != null">
                is_college=#{isCollege},
            </if>
            <if test="address != null and address != ''">
                address=#{address},
            </if>
            <if test="graduationDate != null">
                graduation_date=#{graduationDate},
            </if>
            <if test="violationCount != null">
                violation_count=#{violationCount},
            </if>
            <if test="violationScore != null">
                violation_score=#{violationScore},
            </if>
            update_time=#{updateTime}
        </set>
        where id=#{id}
    </update>
    <update id="updateViolationScore">
        update student
        set violation_score=violation_score+#{score},
            violation_count=violation_count+1
        where id=#{id}
    </update>
    <delete id="deleteBatchIds">
        delete from student where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="selectPage" resultType="com.tk.tliaswebmanagment.pojo.Student">
        select
        s.id,
        s.name,
        s.no,
        s.gender,
        s.phone,
        s.id_card,
        s.is_college,
        s.address,
        s.degree,
        s.graduation_date,
        s.clazz_id,
        s.violation_count,
        s.violation_score,
        s.create_time,
        s.update_time,
        c.name as clazz_name
        from student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%', #{name}, '%')
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>
    <select id="getStudentCountData" resultType="java.util.Map">
        select
            c.name as clazz_name,
            count(s.id) as num
        from student s left join clazz c on s.clazz_id = c.id
        group by c.name
    </select>
    <select id="getStudentDegreeData" resultType="java.util.Map">
        select
            (case s.degree
                when 1 then '初中'
                when 2 then '高中'
                when 3 then '大专'
                when 4 then '本科'
                when 5 then '硕士'
                when 6 then '博士'
                else '其他'
            end) as name,
            count(s.id) as value
        from student s
        group by degree
        order by degree
    </select>
</mapper>